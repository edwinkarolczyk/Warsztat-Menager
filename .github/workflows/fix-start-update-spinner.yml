name: One-shot startup update spinner

on:
  push:
    branches: [Rozwiniecie]
    paths:
      - '.github/workflows/fix-start-update-spinner.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: Rozwiniecie
      - name: Patch start.py
        run: python _patch_start_update_spinner.py
      - name: Commit patch and cleanup
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add start.py
          git rm .github/workflows/fix-start-update-spinner.yml _patch_start_update_spinner.py
          git commit -m "feat: show startup update progress"
          git push origin HEAD:Rozwiniecie
