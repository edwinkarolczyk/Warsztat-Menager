name: One-shot settings save bar

on:
  push:
    branches: [Rozwiniecie]
    paths:
      - '.github/workflows/fix-settings-save-bar.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: Rozwiniecie
      - name: Patch gui_settings.py
        run: python _patch_settings_save_bar.py
      - name: Commit patch and cleanup
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add gui_settings.py
          git rm .github/workflows/fix-settings-save-bar.yml _patch_settings_save_bar.py
          git commit -m "feat: add global settings save bar"
          git push origin HEAD:Rozwiniecie
